# 库存管理系统整体设计文档

## 1. 项目概述
本系统用于管理计算机硬件（整机、显示器、内存、固态硬盘、显卡、WIFI模块等）的采购入库、SN序列号管理、入仓/厂直拆改、退货处理、返修追踪、库存盘点、成本核算及拆改工资计算。采用前后端分离架构，后端Go（Gin + GORM），前端React（Ant Design），数据库MySQL，局域网内可访问。

---

## 2. 核心设计原则
- **SN唯一性**：每个硬件（整机或配件）均以SN作为全局唯一标识，`items`表为库存状态的唯一真相来源。
- **状态同步**：任何业务动作（入库、拆改、退货、返修）必须同步更新`items`表的`current_status`，保证库存随时准确。
- **过程可追溯**：采购单、出库单、退货单、返修单等均为过程记录，不影响库存直接计算，但必须关联SN。
- **成本精确到SN**：每件物品单独记录成本（`unit_cost`），拆改时配件成本随动作转移，工资基于拆改明细计算。
- **前后端分离**：RESTful API，前端负责展示与交互，后端负责业务逻辑与数据校验。

---

## 3. 数据库表设计
### 3.1 产品配置表 `products`
定义标准SKU，用于库存汇总与搜索。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | 主键 |
| product_name | VARCHAR(100) | 商品名称 |
| brand | VARCHAR(50) | 品牌 |
| model | VARCHAR(50) | 型号 |
| category | ENUM('整机','显示器','内存','固态','显卡','WIFI模块','其他') | 类别 |
| cpu | VARCHAR(50) | CPU型号（整机必填） |
| memory | VARCHAR(50) | 内存规格（如16G） |
| storage | VARCHAR(50) | 硬盘规格（如512G SSD） |
| gpu | VARCHAR(50) | 显卡型号 |
| wifi | VARCHAR(50) | 无线网卡型号 |
| min_stock | INT DEFAULT 0 | 库存预警下限 |
| unit_cost | DECIMAL(10,2) | 标准成本参考 |
| unit_price | DECIMAL(10,2) | 标准售价参考 |
| created_at | TIMESTAMP | 创建时间 |

### 3.2 物品表 `items`
每个SN一条记录，库存核心。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| sn | VARCHAR(50) PRIMARY KEY | 序列号（唯一） |
| product_id | INT NOT NULL | 关联products.id |
| name | VARCHAR(100) | 物品名称 |
| category | ENUM(...) | 同products |
| brand | VARCHAR(50) | 品牌 |
| model | VARCHAR(50) | 型号 |
| cpu | VARCHAR(50) | CPU |
| memory | VARCHAR(50) | 内存 |
| storage | VARCHAR(50) | 硬盘 |
| gpu | VARCHAR(50) | 显卡 |
| wifi | VARCHAR(50) | 无线 |
| unit_cost | DECIMAL(10,2) | 成本单价 |
| current_status | ENUM('in_stock','sold','used_in_modification','returned','in_repair','scrapped','pending') | 状态 |
| assembled_into_sn | VARCHAR(50) NULL | 如果被加装到某机器，填写机器SN |
| location | VARCHAR(50) | 库位 |
| remark | TEXT | 备注 |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

**状态说明**：
- `in_stock`：在库
- `sold`：已销售出库
- `used_in_modification`：已被拆改使用（配件已安装到机器上）
- `returned`：退货离开仓库
- `in_repair`：送修中
- `scrapped`：已报废
- `pending`：待处理（如刚采购待校验）

### 3.3 库存流水表 `inventory_transactions`
记录所有SN的库存变动，便于审计。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| sn | VARCHAR(50) | 关联items.sn |
| transaction_type | ENUM('purchase_in','modification_out','modification_in','sale_out','return_in','repair_out','repair_in','scrap_out','other') | 变动类型 |
| quantity | INT DEFAULT 1 | 数量 |
| reference_type | VARCHAR(50) | 关联业务类型（如purchase_order, fulfillment） |
| reference_id | INT | 关联业务ID |
| notes | TEXT | |
| created_at | TIMESTAMP | |

### 3.4 供应商表 `suppliers`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| name | VARCHAR(100) | |
| contact | VARCHAR(50) | |
| phone | VARCHAR(20) | |
| address | VARCHAR(255) | |
| created_at | TIMESTAMP | |

### 3.5 采购订单表 `purchase_orders`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| po_number | VARCHAR(50) UNIQUE | 采购单号 |
| supplier_id | INT | 外键 |
| order_date | DATE | |
| expected_total_qty | INT | 预期总数量 |
| total_amount | DECIMAL(12,2) | |
| status | ENUM('pending','partial','completed','cancelled') | |
| created_at | TIMESTAMP | |

### 3.6 采购入库扫码明细表 `purchase_inbound_items`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| purchase_order_id | INT | 外键 |
| sn | VARCHAR(50) | 扫码到的SN |
| product_name | VARCHAR(100) | |
| category | VARCHAR(50) | |
| unit_price | DECIMAL(10,2) | 该SN单价 |
| scan_time | TIMESTAMP | |
| is_valid | BOOLEAN DEFAULT TRUE | SN格式/重复校验 |
| match_result | BOOLEAN NULL | 数量校验结果（由校验接口更新） |
| created_at | TIMESTAMP | |

### 3.7 出库单表（入仓/厂直）`fulfillments`
统一管理入仓和厂直出库，含拆改状态。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| type | ENUM('inbound','direct') | 入仓或厂直 |
| order_date | DATE | |
| brand | VARCHAR(50) | |
| purchase_order_no | VARCHAR(50) | 入仓用采购单号 |
| delivery_center | VARCHAR(100) | 入仓配送中心 |
| product_code | VARCHAR(50) | 商品编号 |
| product_name | VARCHAR(100) | |
| actual_delivery_qty | INT | 实际送货数量 |
| ordered_qty | INT | 采购单数量 |
| unit_price | DECIMAL(10,2) | |
| ownership | VARCHAR(50) | 归属 |
| channel | VARCHAR(50) | 厂直途径 |
| jd_order_id | VARCHAR(50) | 京东订单号（厂直） |
| customer_name | VARCHAR(50) | 客户姓名 |
| customer_phone | VARCHAR(20) | 客户电话 |
| customer_address | VARCHAR(255) | 客户地址 |
| tracking_number | VARCHAR(50) | 快递单号 |
| is_prepared | BOOLEAN DEFAULT FALSE | 是否备好货（厂直） |
| is_disassembly_completed | BOOLEAN DEFAULT FALSE | 是否完成拆改 |
| sn_qty_match | BOOLEAN | SN数量是否与送货数量一致 |
| price | DECIMAL(10,2) | 最终价格或成本 |
| remark | TEXT | |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

### 3.8 拆改明细表 `disassembly_details`
精确记录每台机器的拆改动作、所用配件SN及成本。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| fulfillment_id | INT | 关联fulfillments.id |
| machine_sn | VARCHAR(50) | 被拆改的机器SN |
| action | ENUM('add','remove') | 加装/拆除 |
| part_sn | VARCHAR(50) | 配件SN |
| part_name | VARCHAR(100) | |
| part_price | DECIMAL(10,2) | 配件成本（从items.unit_cost获取） |
| quantity | INT DEFAULT 1 | |
| staff_name | VARCHAR(50) | 操作人员 |
| created_at | TIMESTAMP | |

### 3.9 工资计算相关表
**工资规则表 `salary_rules`**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| work_type | ENUM('inbound_modification','direct_modification') | 对应fulfillments.type |
| unit_price | DECIMAL(10,2) | 单台拆改工资 |
| description | VARCHAR(100) | |
| created_at | TIMESTAMP | |

**工资记录表 `salary_records`**：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| staff_name | VARCHAR(50) | |
| fulfillment_id | INT | |
| fulfillment_type | ENUM('inbound','direct') | |
| machine_sn | VARCHAR(50) | |
| unit_price | DECIMAL(10,2) | 该台工资单价 |
| quantity | INT DEFAULT 1 | |
| total_amount | DECIMAL(10,2) GENERATED | unit_price * quantity |
| salary_date | DATE | |
| is_paid | BOOLEAN DEFAULT FALSE | |
| created_at | TIMESTAMP | |

### 3.10 退货表 `returns`
统一备件库退货、京东退货、直寄退货。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| source_type | ENUM('spare_parts','jd_return','direct_return') | |
| date | DATE | |
| brand | VARCHAR(50) | |
| outbound_order_no | VARCHAR(50) | 备件库出库单号 |
| spare_barcode | VARCHAR(50) | 备件库条码 |
| sale_return_no | VARCHAR(50) | 直寄退销退单号 |
| jd_order_id | VARCHAR(50) | 订单号 |
| purchase_order_no | VARCHAR(50) | |
| return_order_no | VARCHAR(50) | 京东退货单号 |
| original_sn | VARCHAR(50) | 原SN |
| new_sn | VARCHAR(50) | 处理后的新SN（如更换） |
| sku | VARCHAR(50) | |
| product_name | VARCHAR(100) | |
| tracking_number | VARCHAR(50) | 快递单号 |
| applied_qty | INT | 申请数量 |
| actual_qty | INT | 实际到货数量 |
| unit_cost | DECIMAL(10,2) | 成本 |
| inspection_result | TEXT | 检验情况（划痕、少件等） |
| handling_method | VARCHAR(100) | 处理方式（返回供应商、维修、转二手、报废） |
| is_completed | BOOLEAN DEFAULT FALSE | |
| remark | TEXT | |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

### 3.11 返修表 `repairs`
| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | INT AUTO_INCREMENT | |
| repair_date | DATE | |
| product_name | VARCHAR(100) | |
| sn | VARCHAR(50) | 送修SN |
| quantity | INT DEFAULT 1 | |
| return_address | VARCHAR(255) | 返修地址 |
| return_tracking_no | VARCHAR(50) | 寄回单号 |
| returned_sn | VARCHAR(50) | 返回的SN（可能不同） |
| status | ENUM('sent','repaired','scrapped') | |
| alert_on_stocktake | BOOLEAN DEFAULT TRUE | 盘点时是否警告 |
| disposal_method | ENUM('second_hand','scrap') | 修回后处理 |
| is_completed | BOOLEAN DEFAULT FALSE | |
| remark | TEXT | |
| created_at | TIMESTAMP | |

---

## 4. 后端API设计（RESTful）
Base URL: `http://<host>:8080/api`

### 4.1 库存模块
- **产品库存汇总（搜索）**  
  `GET /inventory/products/summary`  
  参数：`?brand=&model=&category=&cpu=&memory=&storage=&gpu=&wifi=&keyword=&min_stock_alert=true`  
  返回：按产品聚合的库存数量、总成本、预警状态。

- **SN明细列表**  
  `GET /inventory/items`  
  参数：`?product_id=&status=&brand=&sn=&page=&limit=`  
  返回：单个SN信息分页。

- **库存统计**  
  `GET /inventory/statistics`  
  返回：总在库价值、各类别数量等。

- **库存预警**  
  `GET /inventory/alerts`  
  返回：低于最小库存的产品列表。

### 4.2 采购模块
- **采购单CRUD**  
  `GET /purchase-orders`（列表）  
  `POST /purchase-orders`（创建）  
  `GET /purchase-orders/{id}`（详情）

- **扫码入库**  
  `POST /purchase-orders/{id}/items`  
  Body: `{ "sn","product_name","category","unit_price","product_id" }`  
  自动在`items`表创建或校验SN，写入扫码明细。

- **入库校验**  
  `POST /purchase-orders/{id}/verify`  
  后端统计有效扫码数，与`expected_total_qty`比对，更新明细行的`match_result`并返回校验结果。

### 4.3 出库（入仓/厂直）模块
- **出库单CRUD**  
  `GET /fulfillments?type=inbound&status=&date=`  
  `POST /fulfillments`（包含type对应字段）  
  `GET /fulfillments/{id}`

- **提交拆改明细**  
  `POST /fulfillments/{id}/disassembly`  
  Body: `[{ "machine_sn","action","part_sn","part_price","staff" }]`  
  操作：开启事务 → 插入拆改记录 → 更新配件`items`状态（add则变为used_in_modification并设置assembled_into_sn，remove则恢复in_stock并清空assembled_into_sn） → 记录库存流水。

- **标记拆改完成**  
  `PUT /fulfillments/{id}/complete`  
  校验SN数量与`actual_delivery_qty`，更新`is_disassembly_completed`和`sn_qty_match`。

### 4.4 工资模块
- **工资规则**  
  `GET /salary-rules`

- **生成工资**  
  `POST /salary/generate`  
  Body: `{ "fulfillment_id" }` 或批量生成所有已完成未结算的出库单工资。  
  逻辑：根据`fulfillments`中`is_disassembly_completed=true`且对应工资记录未生成时，读取拆改明细，按人员和`salary_rules`创建`salary_records`。

- **工资记录查询**  
  `GET /salary/records?staff=&date=&fulfillment_type=&is_paid=`

### 4.5 退货模块
- **退货单CRUD**  
  `GET /returns?source_type=&status=`  
  `POST /returns`  
  `GET /returns/{id}`

- **处理退货**  
  `PUT /returns/{id}/handle`  
  Body: `{ "inspection_result","handling_method","new_sn","is_completed":true }`  
  处理时更新`items`：原SN状态改为`returned`；若提供了新SN，创建新物品记录（状态`in_stock`），并记录流水。

- **退货中的拆改**：退货检验时若需要拆配件，需调用拆改相关接口？ 可复用：在退货处理时如果涉及拆出配件，同样插入`disassembly_details`（可能需要一个关联退货ID的拆改表，或为退货也创建虚拟fulfillment？）  
  设计简化：退货处理的拆改也记录到`disassembly_details`，并增加`reference_type`和`reference_id`指向`returns.id`。修改`disassembly_details`结构，增加`reference_type`（可`fulfillment`/`return`）和`reference_id`，以适应退货拆改。同时更新`items`状态。

### 4.6 返修模块
- `GET /repairs?status=&alert=`  
- `POST /repairs`  
- `PUT /repairs/{id}`（修回更新，填写`returned_sn`、`disposal_method`，更新`items`状态）

### 4.7 财务模块
- **成本汇总**  
  `GET /finance/cost-summary?start_date=&end_date=&status=`（可多选状态）  
  统计`items`表中相应状态物品的成本总和。

---

## 5. 前端设计
### 5.1 技术栈
React 18 + Ant Design 5，Axios，React Router v6。

### 5.2 路由与页面
| 路径 | 页面组件 | 说明 |
|------|---------|------|
| `/inventory` | InventorySummary | 产品库存汇总表，支持搜索、预警高亮，可点击进入SN明细 |
| `/inventory/items` | ItemList | SN明细，筛选条件、分页 |
| `/purchase` | PurchaseOrders | 采购单列表、创建、扫码入库、校验 |
| `/fulfillments` | FulfillmentList | 出库单列表（Tab切换入仓/厂直） |
| `/fulfillments/:id` | FulfillmentDetail | 出库单详情，拆改明细录入 |
| `/returns` | ReturnList | 退货单列表，新建、处理 |
| `/returns/:id` | ReturnDetail | 退货处理详情，检验、拆改、完成 |
| `/repairs` | RepairList | 返修单管理，高亮未修回 |
| `/salary` | SalaryManagement | 工资规则查看、工资记录、生成结算 |
| `/finance` | FinanceCost | 成本汇总查询 |

### 5.3 核心页面交互说明
- **库存看板**：表格展示产品汇总，列：产品名称、品牌、型号、配置（CPU/内存/硬盘等）、库存数量、总成本、预警状态。搜索栏可输入多个条件。点击数量进入SN明细。
- **采购入库**：创建采购单→在详情页中通过扫码枪或手动输入SN，表格实时显示已扫SN。点击“校验”按钮调用后端校验。
- **出库拆改**：创建出库单→在详情页添加机器SN（主机器），点击“拆改”弹出表单，选择动作（加/拆）、配件SN、操作人。配件SN扫码后系统自动带出配件成本。提交后前端显示已添加的拆改列表。完成所有机器后点击“完成拆改”。
- **退货处理**：新建退货单（选择类型）→在详情页进行检验情况填写，如有拆配件，同样调用拆改组件（复用）。处理方式选择后更新状态。
- **返修看板**：卡片或表格列出所有`status='sent'`的记录，背景黄色高亮。修回操作填写返回SN和处置。
- **工资结算**：查看规则，选择月份或出库单范围，生成工资记录，可标记已支付。
- **财务成本**：选择时间范围、物品状态，展示成本总和及明细。

### 5.4 公共组件
- **SN扫描输入框**：支持扫码枪自动回车，自动查询SN信息并填充。
- **拆改明细组件**：动态表单，支持添加/移除配件行，自动计算成本。
- **状态标签**：统一渲染物品状态（不同颜色）。

---

## 6. 核心业务规则与流程
### 6.1 采购入库与校验
1. 创建采购单（预期数量、供应商等）。
2. 在采购单详情页扫码SN，每扫一个调用`POST /purchase-orders/:id/items`，后端：
   - 检查SN是否存在于`items`表，若不存在则创建（状态`pending`），关联`product_id`。
   - 写入`purchase_inbound_items`。
   - 前端展示已扫SN列表。
3. 点击校验：调用`POST /purchase-orders/:id/verify`，后端统计有效SN数，与`expected_total_qty`比对，更新`match_result`，并返回“匹配/短缺/溢出”。若匹配，可自动将`items`中对应SN状态改为`in_stock`（或手动确认后修改）。

### 6.2 出库（入仓/厂直）及拆改
1. 创建出库单（选择入仓或厂直，填写对应字段）。
2. 在出库单详情中，输入或扫描机器SN（多台），记录到出库单关联表（可增加`fulfillment_items`表存放出库SN，或先只在拆改时体现）。
   - 改进：增加`fulfillment_items`表，列出该单所有的机器SN（包括无需拆改的标机），并记录该机器拆改状态。
3. 对需要拆改的机器，在拆改组件中操作：
   - 加装配件：选择配件SN，系统自动带出配件成本（从`items.unit_cost`），执行后配件状态变为`used_in_modification`，`assembled_into_sn`指向机器SN。
   - 拆除配件：选择机器上已安装的配件（可通过查询`items` where `assembled_into_sn` = 机器SN），拆除后配件状态变回`in_stock`，清空组装关系。
   - 记录拆改明细（`disassembly_details`）和库存流水。
   - 机器本身状态在拆改过程中可保持`in_stock`，或标记为`pending_modification`。
4. 所有机器处理完毕，点击“完成拆改”，校验SN总数是否与`actual_delivery_qty`一致，更新出库单状态，并将所有机器SN状态改为`sold`（或根据业务实际出库状态）。

### 6.3 工资计算
- 根据`salary_rules`，入仓拆改和厂直拆改可能单价不同。
- 生成工资时，系统找出`is_disassembly_completed=true`且尚未生成工资记录（通过关联查询`salary_records`）的出库单，读取拆改明细中每个员工的操作记录，汇总生成工资记录。
- 工资记录可手动标记`is_paid`。

### 6.4 退货处理（含拆改）
1. 创建退货单，记录原SN。
2. 在退货处理详情中：
   - 填写检验情况。
   - 若需从退货机器上拆下配件，使用同样的拆改组件，但关联类型为`return`。拆下的配件恢复为`in_stock`（或根据检验结果定为`scrapped`）。
   - 记录拆改明细（`reference_type='return'`, `reference_id`）。
   - 处理方式选择后，更新原SN状态（如`returned`），若产生新SN（更换），创建新物品`in_stock`。
   - 处理完成标记`is_completed`。
3. 退货中的成本核算：拆下的配件成本回归库存，报废则成本计入损失（可在财务模块特殊处理）。

### 6.5 返修流程
1. 送修：创建返修单，SN状态改为`in_repair`，记录`alert_on_stocktake`=true。
2. 修回：填写`returned_sn`（可能换新SN），选择处置：二手出库（状态`sold`）或报废（`scrapped`）。若返回的SN与原SN不同，需创建新物品记录（状态相应），原SN根据处置改变状态。更新返修单`is_completed`。

### 6.6 成本核算
- 财务查询`items`表，按状态过滤：`sold`,`scrapped`,`used_in_modification`等，汇总`unit_cost`，可按月份（通过流水表或出入库时间）统计。

---

## 7. 安全与部署注意事项
- 局域网内使用，CORS设置允许所有源（开发阶段）。
- 数据库连接信息通过环境变量或配置文件注入。
- 前端build后静态文件可由Go后端直接服务，或独立部署nginx。
- Docker-compose一键启动MySQL和Go应用。

---

## 8. 实施建议
1. 按模块逐步实现：先搭建Go项目框架，创建数据库模型并自动迁移。
2. 实现库存查询模块（最简单），接着采购入库，再出库拆改。
3. 退货、返修最后工资模块。
4. 前端并行开发，可先用Mock数据调试。

---

此文档完整描述了库存管理系统的业务逻辑、数据结构、接口定义及前端设计，可直接交由任何开发人员或AI进行实现。