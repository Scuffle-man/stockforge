

> 本笔记整合了基于 Go + Gin + GORM + MySQL + React 的全栈库存管理系统设计，涵盖业务逻辑、数据库、API、前端、技术要点与部署。适合作为学习参考或直接交予AI实现。

---

## 一、项目概况

- **业务范围**：采购入库、SN 扫码校验、入仓/厂直出库（含拆改）、备件库/京东退货（含拆改）、返修配件、工资计算、库存预警与成本核算。
    
- **技术选型**：Go 1.22+ (Gin + GORM) + MySQL 8.0，前端 React (Ant Design)，Docker 部署。
    
- **核心原则**：
    
    - SN 唯一，`items` 表为库存状态唯一真相来源。
        
    - 所有业务动作需同步更新 `items` 状态，并记录流水。
        
    - 成本精确到 SN，拆改工资与业务解耦。
        

---

## 二、数据库设计

### 1. 产品配置表 `products`

- 用途：定义标准SKU，用于库存汇总及搜索维度。
    
- 字段：
    
    sql
    
    id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    brand VARCHAR(50),
    model VARCHAR(50),
    category ENUM('整机','显示器','内存','固态','显卡','WIFI模块','其他') NOT NULL,
    cpu VARCHAR(50),
    memory VARCHAR(50),
    storage VARCHAR(50),
    gpu VARCHAR(50),
    wifi VARCHAR(50),
    min_stock INT DEFAULT 0 COMMENT '预警下限',
    unit_cost DECIMAL(10,2),
    unit_price DECIMAL(10,2),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    

### 2. 物品表 `items`

- 用途：每个 SN 一条记录，是库存与状态的唯一真相。
    
- 字段：
    
    sql
    
    sn VARCHAR(50) PRIMARY KEY,
    product_id INT NOT NULL,
    name VARCHAR(100) NOT NULL,
    category ENUM(...) NOT NULL,
    brand VARCHAR(50),
    model VARCHAR(50),
    cpu VARCHAR(50),
    memory VARCHAR(50),
    storage VARCHAR(50),
    gpu VARCHAR(50),
    wifi VARCHAR(50),
    unit_cost DECIMAL(10,2),
    current_status ENUM('in_stock','sold','used_in_modification','returned','in_repair','scrapped','pending') DEFAULT 'in_stock',
    assembled_into_sn VARCHAR(50) NULL COMMENT '若配件被加装，记录机器SN',
    location VARCHAR(50),
    remark TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (product_id) REFERENCES products(id)
    

### 3. 库存流水表 `inventory_transactions`

- 用途：记录每次 SN 变动，便于审计与对账。
    
- 字段：
    
    sql
    
    id INT AUTO_INCREMENT PRIMARY KEY,
    sn VARCHAR(50) NOT NULL,
    transaction_type ENUM('purchase_in','modification_out','modification_in','sale_out','return_in','repair_out','repair_in','scrap_out','other'),
    quantity INT DEFAULT 1,
    reference_type VARCHAR(50) COMMENT '关联业务类型',
    reference_id INT COMMENT '关联业务ID',
    notes TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (sn) REFERENCES items(sn)
    

### 4. 供应商表 `suppliers`

- 字段：`id`, `name`, `contact`, `phone`, `address`, `created_at`
    

### 5. 采购单与扫码明细

- **采购订单** `purchase_orders`
    
    - 字段：`id`, `po_number VARCHAR(50) UNIQUE`, `supplier_id`, `order_date DATE`, `expected_total_qty INT`, `total_amount DECIMAL(12,2)`, `status ENUM('pending','partial','completed','cancelled')`, `created_at`
        
- **扫码明细** `purchase_inbound_items`
    
    - 字段：`id`, `purchase_order_id INT`, `sn VARCHAR(50)`, `product_name VARCHAR(100)`, `category VARCHAR(50)`, `unit_price DECIMAL(10,2)`, `scan_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP`, `is_valid BOOLEAN DEFAULT TRUE`, `match_result BOOLEAN NULL`, `created_at`
        

### 6. 出库单（入仓/厂直）`fulfillments`

- 统一管理两类出库，用 `type` 字段区分。
    
- 字段：`id`, `type ENUM('inbound','direct')`, `order_date DATE`, `brand`, `purchase_order_no`, `delivery_center`, `product_code`, `product_name`, `actual_delivery_qty INT`, `ordered_qty INT`, `unit_price`, `ownership`, `channel`, `jd_order_id`, `customer_name`, `customer_phone`, `customer_address`, `tracking_number`, `is_prepared BOOLEAN DEFAULT FALSE`, `is_disassembly_completed BOOLEAN DEFAULT FALSE`, `sn_qty_match BOOLEAN`, `price DECIMAL(10,2)`, `remark TEXT`, `created_at`, `updated_at`
    

### 7. 拆改明细表 `disassembly_details`

- 精确记录每个机器的拆改动作、配件SN及成本，支持多业务类型关联。
    
- 字段：`id`, `reference_type VARCHAR(20) COMMENT 'fulfillment/return'`, `reference_id INT`, `machine_sn VARCHAR(50)`, `action ENUM('add','remove')`, `part_sn VARCHAR(50)`, `part_name VARCHAR(100)`, `part_price DECIMAL(10,2)`, `quantity INT DEFAULT 1`, `staff_name VARCHAR(50)`, `created_at`
    

### 8. 工资相关表

- **工资规则** `salary_rules`
    
    - 字段：`id`, `work_type ENUM('inbound_modification','direct_modification')`, `unit_price DECIMAL(10,2)`, `description`
        
- **工资记录** `salary_records`
    
    - 字段：`id`, `staff_name`, `fulfillment_id`, `fulfillment_type ENUM('inbound','direct')`, `machine_sn`, `unit_price`, `quantity`, `total_amount DECIMAL(10,2) GENERATED`, `salary_date DATE`, `is_paid BOOLEAN DEFAULT FALSE`, `created_at`
        

### 9. 退货表 `returns`

- 统一备件库、京东、直寄退货。
    
- 字段：`id`, `source_type ENUM('spare_parts','jd_return','direct_return')`, `date DATE`, `brand`, `outbound_order_no`, `spare_barcode`, `sale_return_no`, `jd_order_id`, `purchase_order_no`, `return_order_no`, `original_sn VARCHAR(50)`, `new_sn VARCHAR(50)`, `sku`, `product_name`, `tracking_number`, `applied_qty INT`, `actual_qty INT`, `unit_cost DECIMAL(10,2)`, `inspection_result TEXT`, `handling_method VARCHAR(100)`, `is_completed BOOLEAN DEFAULT FALSE`, `remark TEXT`, `created_at`, `updated_at`
    

### 10. 返修表 `repairs`

- 字段：`id`, `repair_date DATE`, `product_name`, `sn VARCHAR(50)`, `quantity`, `return_address`, `return_tracking_no`, `returned_sn VARCHAR(50)`, `status ENUM('sent','repaired','scrapped')`, `alert_on_stocktake BOOLEAN DEFAULT TRUE`, `disposal_method ENUM('second_hand','scrap')`, `is_completed BOOLEAN DEFAULT FALSE`, `remark TEXT`, `created_at`
    

---

## 三、API 设计（RESTful）

Base URL: `/api`

### 1. 库存

- `GET /inventory/products/summary` — 产品库存汇总，支持多条件筛选
    
- `GET /inventory/items` — SN 明细列表
    
- `GET /inventory/statistics` — 全局统计
    
- `GET /inventory/alerts` — 库存预警
    

### 2. 采购

- `GET /purchase-orders` — 列表
    
- `POST /purchase-orders` — 创建
    
- `GET /purchase-orders/:id` — 详情
    
- `POST /purchase-orders/:id/items` — 扫码入库
    
- `POST /purchase-orders/:id/verify` — 校验数量
    

### 3. 出库（入仓/厂直）

- `GET /fulfillments?type=&status=` — 列表
    
- `POST /fulfillments` — 创建
    
- `GET /fulfillments/:id` — 详情
    
- `POST /fulfillments/:id/disassembly` — 提交拆改明细
    
- `PUT /fulfillments/:id/complete` — 标记拆改完成
    

### 4. 工资

- `GET /salary-rules` — 工资规则
    
- `POST /salary/generate` — 生成工资记录
    
- `GET /salary/records` — 查询工资记录
    

### 5. 退货

- `GET /returns?source_type=&status=` — 列表
    
- `POST /returns` — 创建
    
- `PUT /returns/:id/handle` — 处理退货（含拆改）
    

### 6. 返修

- `GET /repairs?status=&alert=` — 列表
    
- `POST /repairs` — 创建
    
- `PUT /repairs/:id` — 修回更新
    

### 7. 财务

- `GET /finance/cost-summary` — 按时间、状态统计成本
    

---

## 四、前端设计（React + Ant Design）

### 1. 路由与页面

|路由|页面|说明|
|---|---|---|
|`/inventory`|InventorySummary|产品汇总，支持搜索，预警高亮，点击进入SN明细|
|`/inventory/items`|ItemList|SN 明细分页，筛选条件|
|`/purchase`|PurchaseOrders|采购单管理、扫码入库、校验|
|`/fulfillments`|FulfillmentList|出库单列表（Tab切换入仓/厂直）|
|`/fulfillments/:id`|FulfillmentDetail|拆改操作|
|`/returns`|ReturnList|退货单管理|
|`/returns/:id`|ReturnDetail|退货处理（含拆改）|
|`/repairs`|RepairList|返修看板，高亮未修回|
|`/salary`|SalaryManagement|工资规则与记录|
|`/finance`|FinanceCost|成本汇总|

### 2. 关键组件

- **SN 扫描输入框**：支持扫码枪回车事件。
    
- **拆改明细组件**：动态表单，加装/拆除，自动带出配件成本。
    
- **状态标签**：颜色标识当前状态。
    

---

## 五、核心业务规则与实现要点

### 1. 采购入库与校验

1. 创建采购单（预期数量）。
    
2. 扫码入库：每扫一个SN调用接口，自动写入扫码明细，并同步创建 `items` 记录（若 SN 不存在），状态 `pending`。
    
3. 校验：统计有效扫码数，与预期数量比对，更新 `match_result`；通过后可将物品状态改为 `in_stock`。
    

### 2. 出库拆改（入仓/厂直）

- 出库单可包含多台机器，通过 `fulfillment_items` 记录（可选）。
    
- 拆改操作：
    
    - **加装**：配件状态变更为 `used_in_modification`，设置 `assembled_into_sn`。
        
    - **拆除**：配件恢复 `in_stock`，清空 `assembled_into_sn`。
        
- 必须使用数据库事务，保证拆改明细与物品状态同时更新。
    
- 拆改完成后校验 SN 总数与 `actual_delivery_qty` 是否一致。
    

### 3. 退货处理（含拆改）

- 退货单关联原 SN。
    
- 检验时可进行拆改：复用拆改组件，`reference_type='return'`。
    
- 拆下的配件恢复库存或报废。
    
- 处理完成后更新原 SN 状态，生成新 SN（如换货）并入库。
    

### 4. 返修流程

- 送修：`items` 状态变为 `in_repair`，设置盘库预警。
    
- 修回：填写返回 SN 及处置（二手/报废），更新对应状态，关闭预警。
    

### 5. 工资计算

- 出库单标记“拆改完成”后，通过生成工资接口，根据拆改明细中的操作人员、工资规则计算每人应得工资。
    
- 生成后的工资记录与后续规则变动无关。
    

### 6. 成本核算

- 财务模块查询 `items` 表中特定状态（sold/scrapped...）的 `unit_cost` 总和，可按时间范围过滤。
    

---

## 六、Go 技术要点

### 1. 模型与 GORM 标签

- 使用 struct 映射表结构，`gorm` 标签定义列属性、索引等。
    
- 枚举用常量定义，string 字段存储。
    

### 2. 查询与聚合

- 动态构建 GORM 查询链，`LEFT JOIN` 产品与物品表实现库存汇总。
    
- 支持多条件组合查询。
    

### 3. 事务处理

go

tx := db.Begin()
defer func() {
  if r := recover(); r != nil {
    tx.Rollback()
  }
}()
if err := tx.Create(&detail).Error; err != nil {
  tx.Rollback()
  return err
}
if err := tx.Model(&Item{}).Where("sn = ?", sn).Updates(...).Error; err != nil {
  tx.Rollback()
  return err
}
tx.Commit()

### 4. 请求验证

- 使用 `binding` 标签验证必填、格式，`ShouldBindJSON` 解析请求。
    

### 5. 中间件

- CORS 中间件允许局域网跨域请求。
    

### 6. 配置管理

- 通过环境变量或 YAML 配置文件加载数据库连接等信息。
    

---

## 七、部署架构

### Docker Compose

yaml

version: '3.8'
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: warehouse
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
  backend:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
    environment:
      DB_DSN: root:rootpass@tcp(db:3306)/warehouse?charset=utf8mb4&parseTime=True
volumes:
  db_data:

Go 多阶段构建 Dockerfile：

dockerfile

FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o warehouse .
FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/warehouse .
EXPOSE 8080
CMD ["./warehouse"]

---

## 八、学习与实现路线

1. **搭建环境**：Go、Docker、MySQL。
    
2. **定义所有模型**：在 Go 中用 struct 定义所有表，配置 AutoMigrate。
    
3. **实现库存查询 API**（最简单入手）。
    
4. **采购入库模块**（扫码、校验）。
    
5. **出库与拆改**（事务、状态同步）。
    
6. **退货与返修**（复用拆改逻辑）。
    
7. **工资模块**（聚合计算）。
    
8. **前端开发**（逐步对接 API）。
    
9. **Docker 部署**：一键启动。
    

---

本笔记涵盖了从需求到设计的完整技术栈与业务逻辑，可直接作为开发文档使用。

这份详解覆盖了从技术选型、数据库设计、业务逻辑算法、Go 代码组织到前端交互的所有关键点。你可以把它当作学习指南，按模块逐个击破，遇到具体的代码实现细节，再结合 AI 或文档进行编写。理解了整体设计后，修改和扩展都会变得有迹可循。还是用上边这些吧。现在我想问你，我应该自己写什么文件比如寻求文档还是什么表，设计文档还是什么表格还是什么东西，我需要把这些东西自己写下来，然后整个的一套，然后后来再去挨个实现，就是软件工程整体的，还有什么架构师反正就往上怼，还有什么实施，还有什么运运维什么的，那就往上怼就完事儿了，就是就当一个整体的开发工程师，全栈开发工程师。这些玩意儿都搞好了，我是可以写在简历里的

我们被要求提供一个完整的软件开发流程文档，包括需求、设计、实现、测试、部署等。用户希望将所有知识点整合成一个专业文档，可以用于简历。需要输出一个结构化的文档，包括：软件工程方法论、架构设计、数据库设计、API设计、前端设计、实施计划、运维等。输出MD格式。

# 库存管理系统 · 全栈开发实战文档

> 本文档按照软件工程标准流程组织，覆盖从需求分析到运维的全生命周期，可作为全栈开发工程师的项目作品，也可直接用于简历展示。

---

## 1. 项目概述

- **项目名称**：计算机硬件库存与拆改管理系统
    
- **业务范围**：采购入库与SN校验、入仓/厂直出库（含拆改）、备件库及京东退货（含拆改）、返修配件、工资核算、库存预警与成本分析。
    
- **用户角色**：库管员、拆改工程师、财务、管理员
    
- **技术栈**：
    
    - 后端：Go 1.22、Gin、GORM、MySQL 8.0
        
    - 前端：React 18、Ant Design 5、Axios
        
    - 基础设施：Docker、Docker Compose、Git
        
- **访问方式**：局域网内Web访问
    

---

## 2. 软件工程方法与流程

本系统采用**敏捷迭代**模式，分阶段交付核心功能：

1. 需求分析与原型设计
    
2. 系统架构与数据库设计
    
3. 后端基础框架搭建与第一个API
    
4. 逐模块开发（采购、库存、出库拆改、退货、返修、工资）
    
5. 前后端联调
    
6. 测试与部署（Docker Compose）
    
7. 文档与交付
    

关键工程实践：

- 版本控制：Git + 分支管理（feature/模块）
    
- 测试：接口测试（Postman/自动化脚本），单元测试（Go testing）
    
- 持续集成（可选）：GitHub Actions 自动构建与镜像推送
    

---

## 3. 需求分析

### 3.1 功能需求

- 采购入库：支持扫码枪录入SN，自动校验数量与SN唯一性。
    
- 库存管理：按品牌/型号/配置多条件搜索，SN级追踪，库存预警。
    
- 出库管理：入仓单与厂直单统一管理，记录拆改明细（加装/拆除配件），成本自动转移。
    
- 退货处理：备件库/京东/直寄退货统一处理，检验、拆改、关联前后SN，状态同步更新。
    
- 返修追踪：送修登记、修回处置（二手/报废），盘点时高亮提示。
    
- 工资计算：根据拆改完成情况与单价规则，自动生成员工工资记录。
    
- 成本核算：按时间段、物品状态汇总成本，辅助财务决策。
    

### 3.2 非功能需求

- 局域网内响应时间 < 200ms
    
- 支持同时 10 个并发用户
    
- 数据每日自动备份（MySQL dump）
    
- 日志记录所有API请求与关键业务操作
    

---

## 4. 系统架构设计

### 4.1 架构风格

- **前后端分离**：RESTful API，JSON数据交换。
    
- **分层架构**：
    
    - 前端层：React SPA，组件化
        
    - 网关层：Nginx（可选，反向代理+静态资源）
        
    - 应用层：Go/Gin，业务逻辑处理
        
    - 数据层：MySQL，GORM ORM
        
- **部署视图**：
    
    text
    
    [用户浏览器] --HTTP--> [Nginx:80] --反向代理--> [Go App:8080] --TCP--> [MySQL:3306]
    所有服务运行在 Docker 容器中
    

### 4.2 技术架构图（文字描述）

- 前端：React (Router, Axios) + Ant Design UI
    
- 后端：Gin (路由/中间件) + GORM (ORM) + MySQL Driver
    
- 存储：MySQL 主库，定期备份至宿主机磁盘
    
- 容器：Docker Compose 编排 `mysql` 与 `backend` 两个服务
    

---

## 5. 数据库设计（详细版）

### 5.1 ER 图简述

- `products` 1──N `items`
    
- `items` 1──N `inventory_transactions`
    
- `suppliers` 1──N `purchase_orders`
    
- `purchase_orders` 1──N `purchase_inbound_items`
    
- `fulfillments` 1──N `disassembly_details` (多态关联至 `returns`)
    
- `returns` 1──N `disassembly_details` (共享拆改表)
    
- `salary_rules` 1──N `salary_records`
    
- `fulfillments` 1──N `salary_records`
    
- `repairs` 独立关联 `items`
    

### 5.2 核心表字段逻辑说明

- `items` 表是**库存真相源**，`current_status` 决定 SN 在库、已用、送修等。
    
- `disassembly_details` 支持多态：`reference_type` (`fulfillment`/`return`) 指明属于出库拆改还是退货拆改。
    
- 工资由 `fulfillments` 完成后生成，后续工资规则修改不影响已生成记录。
    

---

## 6. API 详细设计

### 6.1 通用规范

- 基础路径：`/api`
    
- 认证：暂不实现，局域网内信任环境
    
- 响应格式：`{ "data": ..., "message": "ok" }` 或错误时 `{ "error": "..." }`
    
- 状态码遵循 HTTP 标准
    

### 6.2 接口列表（分类）

#### 库存

- `GET /inventory/products/summary` – 产品维度库存，支持多字段搜索、预警筛选
    
- `GET /inventory/items` – SN 列表（分页），可按状态、产品等过滤
    
- `GET /inventory/statistics` – 库内总价值、各类别数量等
    
- `GET /inventory/alerts` – 低于 `min_stock` 的产品
    

#### 采购

- `POST /purchase-orders` – 创建采购单
    
- `GET /purchase-orders` – 列表
    
- `GET /purchase-orders/:id` – 详情（含已扫SN列表）
    
- `POST /purchase-orders/:id/items` – 扫码添加SN
    
- `POST /purchase-orders/:id/verify` – 校验数量
    

#### 出库（入仓/厂直）

- `POST /fulfillments` – 创建，type字段区分
    
- `GET /fulfillments?type=` – 列表
    
- `GET /fulfillments/:id` – 详情（含拆改明细）
    
- `POST /fulfillments/:id/disassembly` – 添加拆改记录
    
- `PUT /fulfillments/:id/complete` – 标记拆改完成，校验数量
    

#### 退货

- `POST /returns` – 创建
    
- `GET /returns?source_type=` – 列表
    
- `PUT /returns/:id/handle` – 处理退货（检验、拆改、状态更新）
    

#### 返修

- `POST /repairs` – 创建
    
- `GET /repairs?status=` – 列表
    
- `PUT /repairs/:id` – 修回更新
    

#### 工资

- `GET /salary-rules` – 规则列表
    
- `POST /salary/generate` – 手动触发生成工资记录
    
- `GET /salary/records?staff=&is_paid=` – 记录查询
    

#### 财务

- `GET /finance/cost-summary?start_date=&end_date=&status=` – 成本统计
    

---

## 7. 前端设计细节

### 7.1 页面层级与路由

|路由|组件|功能|
|---|---|---|
|`/`|重定向至 `/inventory`||
|`/inventory`|`InventorySummary`|产品汇总表格，支持搜索、预警高亮，点击进入SN详情|
|`/inventory/items?productId=`|`ItemList`|SN 明细表，分页|
|`/purchase`|`PurchaseOrderList`|采购单列表|
|`/purchase/:id`|`PurchaseDetail`|扫码入库、校验|
|`/fulfillments`|`FulfillmentList`|出库单（Tab 切换入仓/厂直）|
|`/fulfillments/:id`|`FulfillmentDetail`|拆改操作|
|`/returns`|`ReturnList`|退货单列表|
|`/returns/:id`|`ReturnDetail`|退货处理（含拆改）|
|`/repairs`|`RepairList`|返修看板，未修回高亮|
|`/salary`|`SalaryManagement`|工资查看与生成|
|`/finance`|`FinanceCost`|成本汇总|

### 7.2 关键交互

- **扫码枪支持**：输入框自动聚焦，监听回车事件，调用API。
    
- **拆改组件**：动态表单，选择“加装/拆除”，扫描配件SN后自动显示名称与成本。
    
- **实时校验**：采购校验按钮后弹窗显示匹配结果。
    
- **状态标签**：用 Tag 组件不同颜色表示物品状态。
    

---

## 8. 核心业务规则与实现逻辑

### 8.1 采购入库校验

- 扫码时即写入 `purchase_inbound_items`，并创建 `items`（状态 `pending`）。
    
- 校验接口统计该单下有效SN数，与 `expected_total_qty` 比对；若匹配可批量更新状态为 `in_stock`。
    

### 8.2 出库拆改（成本转移）

- 事务：插入拆改明细 → 更新配件状态（`used_in_modification` + `assembled_into_sn` 或恢复 `in_stock`）。
    
- 机器成本实时计算或最终统一更新。
    

### 8.3 退货处理

- 复用拆改表，`reference_type='return'`。
    
- 处理时更新原 SN 状态（`returned`），产生新 SN 则新建物品。
    

### 8.4 工资生成

- 找出已完成且未生成工资的出库单 → 读取拆改明细 → 按人员分组 → 按规则计算金额 → 写入 `salary_records`。
    

### 8.5 成本核算

- 直接查询 `items` 表，按 `current_status` 过滤，`SUM(unit_cost)`，支持按流水表关联时间过滤。
    

---

## 9. 项目实施计划

|阶段|任务|预估工时|
|---|---|---|
|基础|项目初始化、Docker环境、数据库表生成|2d|
|后端核心|库存查询、采购入库模块|3d|
|后端扩展|出库拆改、退货、返修|4d|
|后端辅助|工资、财务|2d|
|前端|所有页面开发、联调|5d|
|测试|接口测试、业务流测试|2d|
|部署|Docker Compose、文档|1d|
|**合计**||**约19人天**|

---

## 10. 测试策略

- **单元测试**：Go 中对关键函数（如工资计算、状态转换）编写表驱动测试。
    
- **接口测试**：使用 Postman Collection 覆盖所有 API，验证正常流程与异常情况。
    
- **集成测试**：前后端联调，模拟完整业务流程（采购→入库→出库拆改→退货→返修→工资）。
    

---

## 11. 运维与监控

- **日志**：Gin 内置日志记录每个请求，可输出到文件。
    
- **数据库备份**：cron 定时执行 `mysqldump`，保存到宿主机挂载卷。
    
- **容器健康检查**：Docker Compose 配置 `healthcheck`，监控 MySQL 与 Go 应用。
    
- **版本更新**：修改代码后 `docker-compose build backend && docker-compose up -d` 滚动更新。
    
